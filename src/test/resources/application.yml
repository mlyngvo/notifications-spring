spring:
  main:
    allow-bean-definition-overriding: true

notifications:
  email:
    enabled: true
    debug: true
    smtp:
      host: localhost
      port: 2525
      user: user
      pass: pass
      starttls: true